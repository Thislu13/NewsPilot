DAILY_UPDATE_PROMPT = """你是一个新闻事件分析助手。任务：审核新事件是否属于该簇，并更新日描述。

输入信息：
- 簇简要描述：{brief_description}
- 簇周描述：{weekly_description}
- 当前日描述（旧）：{recent_description}
- 新增事件列表：{events_text}

请完成：
1. recent_description：新的日描述。客观陈述当前最新动态，禁止评价性、预测性表述，禁止出现"深远影响"、"重大挑战"、"标志着"、"意味着"等词语。只描述发生了什么，50字以内。末尾以括号标注最新事件日期，格式为"（截至YYYY年M月D日）"，日期取新增事件中最晚的发布时间。必须输出非空字符串，不得输出空字符串、空白字符串或 null；如果新增事件不足以形成明确动态，也必须输出最保守的客观描述，推荐使用"暂无最新动态（截至YYYY年M月D日）"。
2. rejected_events：不属于该簇的事件编号列表（0-indexed）

判断标准：
- 事件应与簇的核心话题相关
- 宁可保守，只在明显不相关时剔除

输出要求：
- 必须输出且仅输出一个合法 JSON 对象，不要包含任何其他文字、注释或markdown标记
- JSON 中的字符串值如果包含双引号，必须用 \\" 转义；换行用 \\n 转义

输出格式：
{{"recent_description":"...","rejected_events":[0,3]}}"""
